# 森林近地细节与跳跃容错

- 日期：2026-09-07。
- 范围：用户要求继续开发，在原 `chapter-one.html` 推进 F1/F2；没有重做剧情、重置用户存档或增加新地图。本轮没有提交或推送。

## 实际落地

1. 沿实际露出的土壤生成 2–7 px 的小草、蕨叶与落枝。底部查询现有材料块，缺失区块、石头、水中及低顶净空不足处不生成；细节固定于世界坐标，并缓存。它们仅为低矮非阻挡装饰，在人物之前绘制，不新增不可见碰撞。
2. 土层顶边改为连续的苔色／裸土色段，土层内增加细根脉络；没有改变实体轮廓或把装饰伪装成可站立的树根。近景大树、真正的溪岸构图仍未完成。
3. 森林开场启用 100 ms 离地跳跃容错与 120 ms 落地前输入缓冲。按固定模拟时间消耗，离地只允许消耗一次；按住不会反复补充缓冲，不变成二段跳或自动连跳。旧页面的运动调用不启用新容错。
4. 两个短计时器作为可选、有界的 `jumpGrace` 字段随森林空间存档保存并进入校验摘要。旧存档无字段仍可读取；未知子字段、负数、非有限数及超限时间拒绝。检查点回退清空计时器，未触碰原任务和学习奖励。
5. 开发版人物复用旧 v0.6 图集的四个落地帧，每帧 3 tick，总共约 200 ms；从下落接到站立／慢走时播放一次。再次跳起、跑步或互动会中断，不锁输入、不改变碰撞体。帧重复绘制不加速动画，重置／大位移不制造假落地。

## 人物素材尝试：退回，不接入

使用 `imagegen` 技能与内置图像生成工具，以旧 v0.6 为编辑参考生成 v007 动作研究稿。实际文件为 1774×887 RGB，没有透明通道；棋盘背景被画进图片，行列规格和部分接触／交叉姿态也不合格。未通过本次技术检查，不能作为新人物图集交付。

保留原 v0.6 字节和运行时引用。新图、原提示词、SHA-256 和退回理由仅存入私有素材仓，没有复制进公开缓存或发布目录，也没有借机批准旧候选。

私有仓相对路径：

- 图片：`source/art/production/forest-chapter/waterwheel-benchmark/generated-raw/v007/traveler-locomotion-study.imagegen.v007.png`。
- 提示词：`ai/prompts/forest-chapter/traveler-locomotion-study.v007.txt`。
- 技术审核：`manifests/forest-chapter/traveler-locomotion-study.v007.yaml`。
- 本次画面／录像：`review/forest-chapter/opening-framework/2026-09-07/`，详见其 README。

## 验证记录

- 相关单元回归：42 文件、281 项通过；随后补充了旧森林外层存档兼容和未消耗缓冲读档的两项回归，纳入全仓执行。
- 最终全仓单元回归：182 文件、1,152 项通过，约 124 秒，包含上述补充用例。
- 构建与预算通过：初始 JS 1,032,180 B，20 请求；最大分块 324,505 B。原预算未放宽，首屏预算仅余 12 B，下一轮扩充前需要继续拆分非首屏职责。
- 浏览器回归：13/13 通过，约 3.2 分钟。覆盖滚轮／鼠标镜头、窗口变化、三条过溪路线、触控、检查点、存档和坏档保留。
- 私有候选开发画面：Chromium 1440×900，隔离浏览器上下文，新建测试存档。页面错误为空；实际绘制记录确认落地源矩形依次为 `(0,48)`、`(24,48)`、`(48,48)`、`(72,48)`，间隔约 50 ms。画面中人物脚底贴合土层、小植被可见。
- 素材边界：`safe_blocked_pending_external_approval`，公开仓无新生产美术包。旧人物导出 SHA-256 仍为 `6f1ab3cff9313ca2d69a684f632766c11ba23e4bb676b6448a5517b47a7a69ba`。
- 试玩服务重新启动；本机及 Tailscale 地址返回 HTTP 200，外部访问仍需要同一 Tailscale 网络。

## 当前仍未完成

当前仍在 F1/F2；不是第一章完成或美术验收通过。真实水流、连续石木运动、动态材料统一碰撞／存档属于 F3；地下蓝色静态水带还没有迁移。人物走跑原画仍需最终像素整理，起停转向专用动作和近景大构图也未完成。录像与浏览器触控不是实际 Android 性能结论。
