# 第一章森林开场切片验收

> 2026-09-06 状态说明：本文记录现有候选的操作和功能检查，不是 Noita 参照框架的完成证明。[F1–F5 重构](../superpowers/specs/2026-09-06-noita-reference-framework-design.md) 已进入 F1／F2 首版：地表、位移步态、接地和镜头已有实现；预设材料状态及互斥按钮解法仍待迁移。详见[本轮验证记录](./2026-09-06-forest-f1-f2-implementation.md)，自动测试不替代视觉与真实材料交互验收。

## 试玩入口

先运行 `pnpm run assets:dev-sync`，再运行 `pnpm dev`，打开终端显示的
`http://127.0.0.1:<端口>/chapter-one.html`。

这是第一章开场的正式最小切片：旅者从黎明森林抵达受损溪路，选择一种办法通过，观察一处未知刻痕，然后抵达林间聚落边缘。`world-scale.html` 仍是世界尺度与碰撞灰盒，不代表正式试玩画面。

当前公开仓没有通过审批的森林运行时素材包。本地开发服务器会从私有素材库同步并按需加载已生成的旅行者 v0.6 图集及森林远景 v0.3，页面明确显示“本地人物步态候选 v0.6 · 尚未通过正式素材审批”；缺少本地缓存时回退到程序化人物和背景。同步目录受 Git 忽略，生产构建不加载也不复制这些候选素材，因此这仍不等于最终美术验收。

## 操作

- 键盘：`A/D` 或左右方向键移动，`W`、上方向键或空格跳跃，`E` 互动，`F` 观察，`Esc` 暂停。
- `J` 或 HUD“笔记”查看本段旅途；阅读时暂停并清空移动输入。刻痕可选，不观察也能抵达终点。
- 终点“林间落脚”列出过溪办法、见闻和实际保存状态。可选择“临时重玩”，使用独立的本页临时存档，不覆盖原主档；临时笔记或结算中的“返回主进度”可回到主档。
- 鼠标：画面内滚轮向上拉近、向下拉远，范围为默认倍率的 `75%–200%`；游戏画布获得焦点后按 `0` 平滑恢复默认。缩放以人物所在屏幕位置为锚点，缩远时真实扩大地形取样范围，不是缩小旧画面留下黑边。
- 鼠标靠近画面边缘会轻微带动视野，中心有缓冲区；离开画布、切走窗口或暂停后回正。触控不触发鼠标偏移，系统“减少动态效果”偏好关闭该偏移；`Ctrl/⌘ + 滚轮` 保留浏览器缩放。镜头偏好只在当前页面有效，不修改世界或存档，刷新回到默认倍率。
- 横屏触控：左侧方向键移动；右侧“用”“看”“↑”分别对应互动、观察和跳跃。
- 满幅方向输入先进入走路循环，持续同方向约 `0.5 s` 后才切换跑步；动画由实际地面位移推进，不再固定 `12 FPS`。完整左右脚循环的步幅从步行 `32 px` 平滑过渡到满速跑步 `64 px`，八个姿态各至少保留 5 个模拟 tick（60 Hz）。碰墙停住或腾空不继续累计走跑相位。未来虚拟摇杆沿用 `-1…1` 输入轴，轻推只达到步行速度。
- 暂停菜单：“返回检查点”只回退物理位置和未提交局部状态，已经提交的过溪解法不会丢失。
- 右上角“声音：开/关”是独立持久设置；`Esc` 无论焦点在画布还是暂停菜单按钮上都能同步暂停/继续。

一条路线通常需要 1–3 分钟。完整浏览器自动验收包含三种过溪解法、桌面和横屏触控、页面关闭后恢复、坏档恢复与检查点回退，约需 3–5 分钟，取决于机器性能。

## 可验收内容

2026-09-12 稳定性补记：已核验连续三次临时重玩后返回主档，原主档字节不变、人物可见，静止结算不重复改写文字或重画场景；窗口变化与画布恢复通知会触发刷新。全仓 1159 项与开场／镜头 15 项浏览器回归通过，详见[验证记录](./2026-09-12-forest-short-loop-stability.md)。私人候选仅开发版使用，生产版仍是程序化回退；这不替代真实材料、正式美术或 Android 实机验收。

1. 游戏画布覆盖 1440×900 桌面与 844×390 横屏视口，旅者在整段路线中保持可见。
2. 三条互斥路线都能完成同一个语义目标：推动两块松石、拖动枯木、涉水绕行。
3. 兔子与湿地鸟会先依据当前生成地形寻找实体外的合法落点；兔子会逃入遮蔽物，湿地鸟会起飞离开，且不会穿入墙体或阻塞主路线。
4. 未知刻痕只记录“看见过 `telo` 图形”，不会授予词义、发音、学习证据、法术或 MP。
5. 抵达聚落时保持零击杀；关闭页面再进入，结果不重复提交。
6. 坏档保持原字节，不自动新建覆盖；玩家可先导出，再明确重置。
7. 走路与跑步各使用八帧循环，走跑和转向保留左右脚相位；刚按方向键不得直接播放跑步，轻量模拟输入不得被折算成满速。当前仍使用 v0.6 原图集，姿态绘制质量需要单独审核。

## 自动门禁

在 Node 22 环境运行：

```powershell
pnpm run test:forest-opening
```

该命令依次运行森林开场内容/运行时/协调器/存档/视图/音频单测、生产构建、公开素材边界检查和浏览器验收。素材尚未获批时，`assets:check` 应以 `safe_blocked_pending_external_approval` 安全通过边界检查；它不会把候选素材当成已发布素材。

若要单独查看浏览器证据：

```powershell
pnpm run build
pnpm exec playwright test e2e/forest-opening-slice.spec.ts
```

成功运行会在 Playwright 输出目录保留候选阶段的桌面/移动端黎明、障碍与聚落截图；失败时另保留截图、trace 和视频。获批素材一旦缺文件、尺寸错误或清单不合规，正式入口会明确显示“获批素材加载失败”，不会冒充候选待审或悄悄宣布已获批。最终视觉仍需用户审核私有素材候选并完成许可、可访问性与发布审批后才能签字。

需要保留一次无旁白完整试玩视频时，可在 PowerShell 中运行：

```powershell
$env:FOREST_OPENING_CAPTURE_VIDEO = "1"
pnpm exec playwright test e2e/forest-opening-slice.spec.ts -g "mobile complete route"
Remove-Item Env:FOREST_OPENING_CAPTURE_VIDEO
```
